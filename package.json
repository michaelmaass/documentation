{
  "private": true,
  "workspaces": ["yooessentials"],
  "scripts": {
    "start-yooess": "cd ./yooessentials && yarn start",
    "checklinks": "npx linkinator 'src/**/*.md' --markdown --skip '^http?s://' --verbosity error"
  }
}
